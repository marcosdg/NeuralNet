package format;

public class Message {
	
	public static String create(String[] words) {
		StringBuilder message = new StringBuilder();
		
		for (int at = 0; at < words.length; at += 1) {
			message.append(words[at]); 
		}
		return message.toString();
	}

}
