package core.activation;

/* Base class for all activation functions. 
 */
public abstract class ActivationFunction {

	// Takes as parameter the result from Propagation function.
	
	abstract double getOutput(double netInput);
}
